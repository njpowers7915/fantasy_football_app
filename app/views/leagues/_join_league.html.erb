<%= form_for team do |f| %>
  <%= f.hidden_field :name %><br>
  <%= f.hidden_field :user_id %><br>
  <%= f.hidden_field :league_id %><br>
  <input type='hidden' id="join_league" name="join_league" >

  <%= f.submit "JOIN" %>
<% end %>

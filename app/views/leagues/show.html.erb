<h2> <%= @league.name %> </h2><br>

<% if is_admin? %>
  <h5><%= link_to "Edit League Info", edit_league_path(@league) %></h5>
  <br>
<% end %>


<h4> League Standings </h4>
<table style='width:50%'>
  <tr>
    <th>Position</th>
    <th>Team Name</th>
    <th>Points</th>
    <th>User</th>
  </tr>

  <% @teams.each_with_index do |team, i| %>
  <tr id="team_profile" team_id="<%= team.id %>">
    <td><%= i + 1 %></td>
    <td><%= team.name %></td>
    <td><%= team.points_total %></td>
    <td><%= team.user.name %></td>
    </tr>
    <% end %>
  </table>
  <br>

<h4><%= link_to "Message Board", league_comments_path(@league, @comments) %></h4>
